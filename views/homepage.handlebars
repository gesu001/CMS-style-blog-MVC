{{!-- {{#each posts as |post| }}
<div>
    <div>
        <h2>
            <a href="/post/{{post.id}}">{{post.title}}</a>
            <span>Created by {{post.user.username}} on {{format_date post.date_created}}</span>
        </h2>
    </div>
    {{!-- <div>
        <p>
            {{post.content}}
        </p>
    </div> --}}
{{!-- </div>
{{/each}} --}} 

<ol class="list-group list-group-numbered">
    {{#each posts as |post| }}
  <li class="list-group-item d-flex justify-content-between align-items-start">
    <div class="ms-2 me-auto">
      <div class="fw-bold"><a href="/post/{{post.id}}">{{post.title}}</a></div>
    </div>
    <span class="badge bg-primary rounded-pill">Created by {{post.user.username}} on {{format_date post.date_created}}</span>
  </li>
  {{/each}}

</ol>